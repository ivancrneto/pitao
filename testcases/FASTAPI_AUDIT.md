# FastAPI Round-Trip Audit

Automatically generated by `tests/audit_fastapi_roundtrip.py`. Do not edit by hand; regenerate with `--write`.

## Methodology

For every `.py` file in the installed framework source we compute the round trip `py -> pt -> py` (`translate_keywords` reverse then forward) and count lines that are not recovered byte-for-byte. Each corrupted token is bucketed by *why* it broke.

- Packages audited: `fastapi` 0.138.0
- Python `.py` files scanned: **48**

## Before / after the attribute-corruption fix

`before` = keyword pass with a plain `\b` boundary (pre-fix). `after` = keyword pass with a `(?<!\.)` lookbehind plus the path predicates moved to the dotted method pass (current code).

| Metric | before | after |
| --- | ---: | ---: |
| Files corrupted (of 48) | 6 | 6 |
| Corrupted lines | 39 | 34 |
| &nbsp;&nbsp;attribute_corruption | 5 | 0 |
| &nbsp;&nbsp;datetime_shortword | 18 | 18 |
| &nbsp;&nbsp;single_letter_id | 19 | 19 |

## Corruption classes (after the fix)

- **attribute_corruption** - 0 line(s) after fix. Examples: `data -> date`
- **datetime_shortword** - 18 line(s) after fix. Examples: `data -> date`
- **single_letter_id** - 19 line(s) after fix. Examples: `e -> and`

## Fixable vs. inherent

- **Fixed (regex-feasible):** `attribute_corruption` - a `(?<!\.)` lookbehind on the keyword pass stops `obj.data` becoming `obj.date` and `x.type` becoming `x.tipo`. Path predicates (`existe`/`ehdir`/`eharq`) were miscategorised as keywords and were moved into the dotted method pass, where they round-trip correctly.
- **By design:** the FastAPI fluent API `.get` maps to `.obter`, so `@app.get` round-trips through the Portuguese spelling. Pitão authors write `@app.obter`; this is lossless within Pitão's model.
- **Inherent (NOT regex-fixable without scope analysis):** `single_letter_id` such as `e -> and` (this turns `except ... as e:` into `except ... as and:`, a `SyntaxError` - the highest-severity class), `datetime_shortword` such as `data -> date`, and `builtin_as_identifier` such as a variable named `type`/`id`. A bare identifier is indistinguishable from a keyword by regex alone; the only true fix is an AST/scope-aware translation pass. Mitigation today is authoring discipline in `.pt` files (see `testcases/fastapi_app.pt`).

