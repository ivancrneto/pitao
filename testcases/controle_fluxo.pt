# Demonstração de controle de fluxo em Pitão

def fibonacci(n):
    """Calcula o n-ésimo número de Fibonacci."""
    se n <= 0:
        retorne 0
    senaose n == 1:
        retorne 1
    senao:
        retorne fibonacci(n - 1) + fibonacci(n - 2)

def fatorial(n):
    """Calcula o fatorial de n."""
    resultado = 1
    para i em range(1, n + 1):
        resultado = resultado * i
    retorne resultado

def contador(limite):
    """Conta até o limite."""
    i = 0
    enquanto i < limite:
        imprimir(f"Contando: {i}")
        i = i + 1

def buscar_item(lista, alvo):
    """Busca um item em uma lista."""
    para item em lista:
        se item == alvo:
            imprimir(f"Encontrado: {item}")
            quebre
    senao:
        imprimir("Item não encontrado")

se __nome__ == "__principal__":
    imprimir("=== Fibonacci ===")
    para n em intervalo(10):
        imprimir(f"fib({n}) = {fibonacci(n)}")
    
    imprimir("\n=== Fatorial ===")
    para n em intervalo(1, 8):
        imprimir(f"{n}! = {fatorial(n)}")
    
    imprimir("\n=== Contador ===")
    contador(5)
    
    imprimir("\n=== Busca ===")
    frutas = ["maçã", "banana", "laranja"]
    buscar_item(frutas, "banana")
    buscar_item(frutas, "uva")
